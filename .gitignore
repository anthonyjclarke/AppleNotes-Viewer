# Notes export data — never commit personal notes
# The Notes/ directory itself is tracked (via .gitkeep); its contents are not
Notes/*
!Notes/.gitkeep

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Python
__pycache__/
*.py[cod]

# Claude Code internal
.claude/launch.json
